[project]
name = "shuntaka-dev"
version = "0.1.0"
requires-python = ">=3.14.6"
dependencies = []

[tool.uv.workspace]
members = ["docs"]

[tool.uv.sources]
docs = { workspace = true }

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple/"
default = true

[dependency-groups]
dev = ["docs"]
