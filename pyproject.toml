[project]
name = "shuntaka-dev"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = []

[tool.uv.workspace]
members = ["docs"]

[tool.uv.sources]
docs = { workspace = true }

[dependency-groups]
dev = ["docs"]
