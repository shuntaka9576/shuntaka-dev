[project]
name = "shuntaka-dev"
version = "0.1.0"
requires-python = ">=3.14.3"
dependencies = []

[tool.uv.workspace]
members = ["docs"]

[tool.uv.sources]
docs = { workspace = true }

[dependency-groups]
dev = ["docs"]
