{
  "name": "@shuntaka-dev/aws",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "deploy": "dotenv -- cdk deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update": "vitest run --update"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.237.1",
    "change-case": "^5.4.4",
    "constructs": "^10.4.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "aws-cdk": "^2.1105.0",
    "dotenv-cli": "^11.0.0",
    "vitest": "^4.0.18"
  }
}
